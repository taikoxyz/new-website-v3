### S03: Shared UI Components
Status: in_progress
Depends on: S01

Description:
Port all shared UI components from the source repo (/Users/gustavo/taiko/website-v3). Components to port: Button, ArrowButton, BackLink, Accordion, Input, Select, Select-2, MultiSelect, Modal (with provider), FileInput, DragFile, Image (Next.js Image wrapper), NextLink (Next Link wrapper), ShareButton, MediaQuery (responsive conditional rendering), MediaLoader (lazy loading), Sprite (SVG sprite renderer), SubscribeSection (newsletter form), Socials (social media icons), Label, TextSplitter, LinkDropdown, RangeCalendar, RichText (replaces Strapi Blocks renderer — render MDX/markdown content), AlertCard, Banner, Grants. Use SCSS Modules for styling. Adapt from Pages Router patterns to App Router (remove withTranslation HOCs, use 'use client' where needed).

Acceptance Criteria:
- [ ] All listed UI components are ported with SCSS Module styling
- [ ] Components use 'use client' directive where needed for interactivity
- [ ] No withTranslation or i18n HOC dependencies remain
- [ ] RichText component can render markdown/MDX content
- [ ] TypeScript compiles with no errors
