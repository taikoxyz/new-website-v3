You are an autonomous coding agent.
Use the $prd skill to create a Product Requirements Document in JSON.
Save the PRD as JSON in directory: /Users/gustavo/apps/new-website-v3/.agents/tasks
Filename rules: prd-<short-slug>.json using 1-3 meaningful words.
Examples: prd-workout-tracker.json, prd-usage-billing.json
Do NOT implement anything.
After creating the PRD, end with:
PRD JSON saved to <path>. Close this chat and run `ralph build`.

User request:
Copy https://taiko.xyz/ bit by bit and content by content in a new app. The original source code is available in /Users/gustavo/taiko/website-v3. Our goal is that we DON'T use a CMS like strapi, but instead all contents is in the repo itself. Pick the latest versions of the framework and libraries, and make sure the result looks exactly like the original website. We'll deploy the site to vercel.
